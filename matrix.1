# Now learing about matrix which are 2 dimensional
vec1 <- 1:9
m <- matrix(vec1, nrow = 3)
# need to specifiy rows 
m
# square brackets and position like 1 dimensional objects so we need to specify 2 dimensions row x and column y
# put two number and seprate with comma first comes the rows then the columns when coding
class(m)
typeof(m)
# tells you if it is an interger or other values
m <- matrix(LETTERS[1:9], 
            ncol = 3)
m
# we dont want to fill by columsn but ROWS transpose and do rotation
t(m) #but suggested not to do this and build it instead without modfying later

m <- matrix(data = LETTERS[1:9], 
            ncol = 3,
            byrow = TRUE)
m
first_row <- 1:5
second_row <- 11:15
third_row <- 101:105
rbind(first_row,
      second_row,
      third_row)

first_col <- 1:5
second_col <- 11:15
third_col <- 101:105
cbind(first_col,
      second_col,
      third_col)
 length(m)
nrow(m) 
ncol(m)
m <- matrix(1:6 , ncol = 3)
m
dim(m)
# dim refers to rows and columns
rownames(m)
rownames(m) <- c("r1", "r2")
m
colnames(m) <- c("c1", "c2", "c3")
m
rownames(m) <- NULL #removes assignment
m[2,1]
m[1, ] # extract first row hold columns
vec1[1:3]
m[, 2:3] # extracts the 
m[, c(1, 3)]
m[, -2]

m["r1", c(1,3)] 
#first row and column indices
m[1:2, 2:3]
m
m[, c(TRUE, FALSE, TRUE)]
m[1,1]<- 9 # how to modify value with vector.s
m[,3] <- 6
m
m[, 2] <- c(7, 9, 10)
m[-1,]# minus row they must be rectangular
m + 2
m / 2 

%*% # matric multiplication
